<input
    name="quantity"
    id="{{item.id}}-count"
    value={{item.quantity}}
    min="1"
    max={{item.stock}}
    type="number"
    hx-trigger="change throttle:300ms"
    hx-patch="/cart/{{item.id}}"
    hx-swap="outerHTML"
/>

<div id="total-info" hx-swap-oob="true">
    <h4>Sub-total:</h4>
    <p>{{total}} ₽</p>
</div>